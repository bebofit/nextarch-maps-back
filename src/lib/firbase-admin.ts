// import config from '../config';
// import * as admin from 'firebase-admin';

// const { FIREBASE_ADMIN } = config;

// const customAdmin = admin.initializeApp({
//   credential: admin.credential.cert(JSON.parse(FIREBASE_ADMIN)),
//   databaseURL: 'https://grok-app.firebaseio.com'
// });

// export default customAdmin;
